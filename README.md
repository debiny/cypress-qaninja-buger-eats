# cypress-qaninja-buger-eats


### npx cypress open
#### Execução com a interface gráfica do cypress (configurada no package.json) 

### npx cypress run 
#### Executa em headless

### npx cypress run -b chrome
#### Executa no chrome
